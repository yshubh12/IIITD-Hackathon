const abi = require("./abi.json")
const contractAddress = require("./contractAddress.json")
const abiFundMe = require("./abiFundMe.json")

module.exports = {
    abi,
    contractAddress,
    abiFundMe
}